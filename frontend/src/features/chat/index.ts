// The chat feature's public surface. Import from "./features/chat",
// never reach into its internal files directly.
export { ChatPanel } from "./components/ChatPanel";
export type { Message, MessageRole } from "./types";
